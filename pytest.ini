[pytest]
# Repo root on path for `import agent` (tests live under tests/unit/trading_agent_tests/).
pythonpath = .
# Avoid shadowing repo packages `backend` and `tools` by tests/unit/backend and tests/unit/tools.
addopts = --import-mode=importlib
